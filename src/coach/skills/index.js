/**
 * Coach Skills Framework
 * 
 * Skills are markdown files in specs/coach-skills-system/
 * Coach reads these files directly - no loaders, no classes needed.
 */

// Export skill interface if needed elsewhere
export { Skill } from '../skill-interface.js';
